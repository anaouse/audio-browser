# send absolute file path through ipc

example: `D:\\projecst\\audio-browser\\audio\\clap-808.wav`

# uv python env

use `uv` instead of `python`

`uv python -c`

`uv pip list`

# 核心逻辑

- 点击 tree node
- 获取鼠标位置，发出声音
- 把窗口移过来
-
